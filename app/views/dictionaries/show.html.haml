%h1= "Dictionary: #{@dictionary.name}"
%p
  = link_to 'Quick add', edit_dictionary_path(@dictionary)
  |
  = link_to 'Merge duplicates', merge_duplicates_dictionary_path(@dictionary)
  |
  = link_to 'Back to all dictionaries', dictionaries_path
%table{ width: '100%' }
  %thead
    %tr
      %th Dictionary
      %th Initialism
      %th Meaning
      %th{:colspan => "3"} Actions
  %tbody
    - @dictionary.definitions.sort.each do |definition|
      = render definition
